---
id: T-004
name: "Cache Redis nas leituras do Core"
type: FEATURE
difficulty: medium
estimate: "1d"
services: [core]
tags: [nestjs, redis, cache, performance]
---

## Contexto

As rotas de leitura do Core (`GET /products`, `GET /products/:id`) são chamadas com alta frequência
pelo BFF e não possuem nenhuma camada de cache. Cada requisição resulta em uma query ao MongoDB.

O Redis já está declarado no `docker-compose.yml` e disponível em `redis:6379`, mas não está sendo
utilizado pelo código.

## Escopo

- Instalar e configurar `@nestjs/cache-manager` com o driver Redis (`cache-manager-redis-yet`) no Core
- Aplicar cache com TTL de 60 segundos nas respostas de `GET /products` (lista completa)
- Aplicar cache com TTL de 120 segundos nas respostas de `GET /products/:id` (produto individual)
- Invalidar o cache de lista de produtos quando um novo produto for criado via `POST /products`
- As variáveis de conexão com Redis devem vir de variáveis de ambiente (`REDIS_HOST`, `REDIS_PORT`)

## Dicas

- `CacheModule.registerAsync` no `AppModule` ou `ProductsModule` para configuração via env
- `@nestjs/cache-manager` expõe o decorator `@UseInterceptors(CacheInterceptor)` para cache automático
  e o `CACHE_MANAGER` token para cache manual com `cacheManager.set/get/del`
- A chave de cache de lista pode ser `'products:all'`; a de produto individual, `'products:<id>'`
- Cache manual (via `CACHE_MANAGER`) dá mais controle sobre invalidação do que o interceptor automático

## Fora do escopo

- Não aplicar cache em `orders` ou `customers`
- Não implementar cache no BFF
- Não alterar o TTL do cache via endpoint ou config dinâmica
- Não usar Redis como fila ou pub/sub nesta task

## Definition of done

- [ ] `GET /products` retorna resposta cacheada no segundo acesso (verificável via log de queries ao MongoDB reduzido)
- [ ] `GET /products/:id` retorna resposta cacheada no segundo acesso
- [ ] `POST /products` invalida o cache da lista
- [ ] Conexão com Redis configurada via `REDIS_HOST` e `REDIS_PORT`
- [ ] Há testes unitários cobrindo o comportamento de cache e invalidação

## Domain

- `core/src/products/products.service.ts`
- `core/src/products/products.module.ts`
- `core/src/app.module.ts`
