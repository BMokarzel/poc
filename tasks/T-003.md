---
id: T-003
name: "Comunicação via GraphQL entre BFF e Core"
type: FEATURE
difficulty: medium
estimate: "1d"
services: [bff, core]
tags: [nestjs, graphql, apollo]
---

## Contexto

Atualmente o BFF se comunica com o Core exclusivamente via HTTP REST, usando `CoreHttpClient`.
Essa abordagem exige múltiplas requisições para compor respostas que agregam dados de diferentes recursos
(ex: pedido com detalhes de produtos e cliente).

O time decidiu adotar GraphQL como protocolo de comunicação entre BFF e Core para permitir que o BFF
requisite exatamente os campos necessários em uma única chamada.

## Escopo

- Configurar o Apollo Server no Core com `@nestjs/graphql` (schema-first ou code-first — escolha justificada no commit)
- Expor um `ProductResolver` no Core com a query `products: [Product!]!` e `product(id: ID!): Product`
- Expor um `OrderResolver` no Core com a query `orders: [Order!]!`
- Configurar o Apollo Client no BFF para realizar queries GraphQL ao Core
- Substituir as chamadas REST de `products` e `orders` no BFF por queries GraphQL
- A comunicação HTTP REST entre BFF e Core deve continuar funcionando para o módulo `customers` (não migrar)

## Dicas

- `@nestjs/graphql` e `@apollo/server` para o servidor; `@apollo/client` ou `graphql-request` para o cliente no BFF
- No Core, o endpoint GraphQL fica em `POST /graphql` por padrão
- Code-first gera o schema a partir dos decorators TypeScript — mais alinhado com o padrão NestJS do projeto
- Atenção ao `autoSchemaFile` no `GraphQLModule.forRoot` para geração automática do schema

## Fora do escopo

- Não implementar mutations GraphQL (apenas queries)
- Não migrar o módulo `customers` para GraphQL
- Não substituir a API REST do Core (deve continuar existindo em paralelo)
- Não implementar subscriptions

## Definition of done

- [ ] `POST /graphql` no Core responde à query `{ products { id name price } }`
- [ ] `POST /graphql` no Core responde à query `{ product(id: "...") { id name } }`
- [ ] `POST /graphql` no Core responde à query `{ orders { id status total } }`
- [ ] `GET /products` no BFF continua funcionando e usa GraphQL internamente para buscar dados do Core
- [ ] `GET /orders` no BFF continua funcionando e usa GraphQL internamente para buscar dados do Core
- [ ] `GET /customers` no BFF continua funcionando via REST (não migrado)
- [ ] Há testes unitários cobrindo os resolvers do Core

## Domain

- `core/src/products/products.resolver.ts` (novo)
- `core/src/orders/orders.resolver.ts` (novo)
- `core/src/app.module.ts`
- `bff/src/common/graphql/` (novo)
- `bff/src/products/products.service.ts`
- `bff/src/orders/orders.service.ts`
