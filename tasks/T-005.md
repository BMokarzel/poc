---
id: T-005
name: "Observabilidade com OpenTelemetry, Jaeger, Prometheus e Grafana"
type: FEATURE
difficulty: hard
estimate: "3d"
services: [bff, core]
tags: [opentelemetry, jaeger, prometheus, grafana, observability, tracing, metrics, logs]
---

## Contexto

Os dois serviços não possuem nenhuma instrumentação de observabilidade. Não há traces distribuídos,
métricas de performance nem logs estruturados correlacionados com requisições.

Quando uma requisição lenta ou com erro ocorre, não é possível determinar em qual serviço o problema
se originou, quanto tempo cada etapa levou ou qual foi o contexto da falha.

## Escopo

- Configurar o OpenTelemetry SDK em ambos os serviços (BFF e Core) com auto-instrumentação para HTTP e MongoDB
- Exportar traces para o Jaeger via OTLP (HTTP ou gRPC)
- Exportar métricas (latência, contagem de requisições, erros) para o Prometheus via endpoint `/metrics`
- Adicionar o Jaeger e o Prometheus ao `docker-compose.yml`
- Criar um dashboard básico no Grafana com ao menos: latência p50/p95 por rota, taxa de erros e throughput
- Adicionar o Grafana ao `docker-compose.yml` apontando para o Prometheus como datasource
- Emitir logs estruturados (JSON) correlacionados com `trace_id` e `span_id` nos endpoints de criação (`POST /orders`, `POST /products`, `POST /customers`)

## Dicas

- `@opentelemetry/sdk-node` e `@opentelemetry/auto-instrumentations-node` para setup simplificado
- O SDK deve ser inicializado antes do bootstrap do NestJS — arquivo separado `src/instrumentation.ts` importado via `--require` ou como primeiro import do `main.ts`
- `@opentelemetry/exporter-trace-otlp-http` para envio de traces ao Jaeger
- `@opentelemetry/exporter-prometheus` expõe o endpoint `/metrics` no formato Prometheus
- Jaeger aceita OTLP na porta 4318 (HTTP); imagem `jaegertracing/all-in-one`
- Prometheus scrape config aponta para `<serviço>:9464/metrics` (porta padrão do exporter)
- Grafana: imagem `grafana/grafana`, provisionar datasource Prometheus via arquivo YAML em `/etc/grafana/provisioning/datasources/`

## Fora do escopo

- Não implementar alertas no Grafana
- Não configurar retenção ou persistência de dados do Prometheus
- Não instrumentar queries Redis (somente HTTP e MongoDB)
- Não implementar sampling customizado (usar o default)

## Definition of done

- [ ] Traces aparecem no Jaeger UI para requisições ao BFF e ao Core
- [ ] Um trace de `GET /products` no BFF mostra o span filho da chamada ao Core
- [ ] Métricas de latência e contagem de requisições estão disponíveis em `GET /metrics` em ambos os serviços
- [ ] O Prometheus coleta as métricas dos dois serviços
- [ ] O Grafana exibe ao menos um gráfico de latência e um de throughput com dados reais
- [ ] Logs de `POST /orders` contêm `trace_id` e `span_id`
- [ ] Jaeger, Prometheus e Grafana estão no `docker-compose.yml` e sobem com `docker compose up`

## Domain

- `bff/src/instrumentation.ts` (novo)
- `bff/src/main.ts`
- `core/src/instrumentation.ts` (novo)
- `core/src/main.ts`
- `core/src/orders/orders.service.ts`
- `core/src/products/products.service.ts`
- `core/src/customers/customers.service.ts`
- `backend/docker-compose.yml`
- `backend/grafana/` (novo — provisioning)
